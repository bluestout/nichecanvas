{{ 'option_selection.js' | shopify_asset_url | script_tag }}

<div class="{% unless page.handle == "help-center"%}page-width{%endunless%}">
     {{ page.content }} <!-- include 'wishlist-collection' -->
</div>
